import _Vue from 'vue'

/**
 * When used with a module system,
 * you must explicitly install Logux Vuex via `Vue.use()`
 *
 * ```
 * import Vue from 'vue'
 * import { LoguxVuex } from 'logux/vuex'
 *
 * Vue.use(LoguxVuex)
 * ```
 *
 * @param Vue Vue instance
 */
export declare function LoguxVuex(Vue: typeof _Vue): void
